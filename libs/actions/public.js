const assert = require('assert')

module.exports = (config,libs) => user =>{
  function echo(x){
    return x
  }
  function logout(){
  }
  function token(){
  }

  return {
    echo,
    logout,
    token
  }
}

